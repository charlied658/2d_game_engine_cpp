/**
 * This acts as the implementation for stb_image using preprocessor directives.
 * Note: The file "stb_image.h" is not included in this project because it is too large and I did not write it
 * To properly compile this project, download the file "stb_image.h" from
 * https://github.com/nothings/stb/blob/master/stb_image.h
 * and place it into the path: include/lib/stb_image.h within this project.
 * Thanks.
 */

#define STB_IMAGE_IMPLEMENTATION
#include "lib/stb_image.h"
